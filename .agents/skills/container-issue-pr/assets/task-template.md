# Task

Issue URL:
Source base commit:

## Scope

Describe the concrete behavior to implement and the boundaries established from
the Issue, comments, repository instructions and specifications.

## Acceptance criteria

List observable outcomes. Map each to the host commands or runtime operations
that will verify it. Record dependencies and confirm their code is in the base.

## Container work

Use the already allocated Issue worktree in the current directory. Read this
repository's instructions in the context of that assignment; creation of the
worktrees, host acceptance and PR publication belong to the coordinator.
The checkout is a filtered host snapshot with its own Git history. Record
implementation results and remaining concerns in your final response. Host
runtime verification and PR publication are performed by the coordinating agent.

## Host handoff

Specify setup, build, launch, operations, expected screen/state, cleanup, and
evidence to collect. Identify required device IDs, sessions, ports and locks.

## Completion

All acceptance criteria implemented; describe changed files and any checks run.
Successful container checks are preliminary; the host must verify the result.
