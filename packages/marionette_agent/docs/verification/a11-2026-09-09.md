# A11: レビュー指摘2件の修正

担当: GPT-6 Astra。2026-09-09。

## CLI起動時間

artifact_writer.dartがimageの公開barrelをimportすると、screenshot以外のCLIでも全codec/filterの依存を読み込む。PNG専用`PngDecoder`へimportを限定し、復号・PNG検証を維持した。内部API利用はこのファイルに閉じ、imageを4.9.1へ完全固定。内部importの例外理由と更新時にPNG契約テストが必要なことをコード・実装契約へ記載した。

同じソースのwriter importだけを旧barrel版へ戻した一時runnerと、修正版を交互に3回測定した。Dart SDKで`--packages=<package_config.json> <entrypoint> --version`を実行し、全6回exit 0を確認。

| 起動方式 | 1回目 | 2回目 | 3回目 | 中央値 |
| --- | --- | --- | --- | --- |
| 旧barrel import | 1.418s | 1.463s | 1.452s | 1.452s |
| PNG専用import | 0.880s | 0.888s | 0.872s | 0.880s |

中央値で約39%短縮。PNG専用decoderの読み込み負荷は通常CLIにも一部残る。数値は同一macOS環境のDartソース実行であり、コンパイル済みバイナリの起動時間ではない。[測定値](a11-benchmark.json)。

## workflowのstep ID

workflow-file-spec.mdのstep ID制約を`^[A-Za-z][A-Za-z0-9_-]{0,63}$`と明記し、ハイフンを許可した。input名の制約は変更していない。YAML/JSONの全5サンプルIDを抽出し、すべて新しい制約に適合することを確認した。

## 検証

- パッケージ内で`dart pub get --offline`、`dart format .`成功、`dart analyze`指摘なし、`dart test`全64件合格。
- iPhone 17 Pro Max / iOS 26.2、Flutter 3.47.2、binding 0.6.0のoperation_confirmationで実CLI検証。

```sh
# operation_confirmation内
flutter run -d 59AA2C66-0A43-4E6B-9B63-63B7E38F72F9 --debug --no-pub \
  --vmservice-out-file=/tmp/mra-a11-uri
# packages/marionette_agent内
MARIONETTE_TEST_VM_URI_FILE=/tmp/mra-a11-uri \
MARIONETTE_TEST_EVIDENCE=/tmp/mra-a11-checks/results.json \
  dart run integration_test/features_smoke.dart
```

30回のCLI呼出しが期待終了コードに一致し、runnerがPASS。tapのカウンタ更新、fill置換・クリア、scroll後のBottom reached、ログ、PNG復号・一時保存・上書き拒否と元bytes保持・ref保持を確認した。[CLI結果](a11-results.json)と[保存されたPNG](a11-screens/scrolled.png)。PNGを開き、Bottom reachedとスクロール後の状態を目視確認した。

検証session/daemonはrunnerが終了処理を行い、Flutter runnerはdetach。認証URI・Flutterログ・比較用runnerの一時ファイルを削除した。
